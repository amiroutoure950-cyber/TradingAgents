const form=document.querySelector('#analysis-form');const symbol=document.querySelector('#symbol');const date=document.querySelector('#date');const pairLabel=document.querySelector('#pair-label');const launch=document.querySelector('.launch');const empty=document.querySelector('#empty-state');const progress=document.querySelector('#progress-state');const report=document.querySelector('#report-state');const historyView=document.querySelector('#history-view');const historyList=document.querySelector('#history-list');const historyCount=document.querySelector('#history-count');const progressTitle=document.querySelector('#progress-title');const progressMessage=document.querySelector('#progress-message');let timer=null;let currentHistory=[];date.value=new Date().toISOString().slice(0,10);function updatePair(){const p=symbol.value.toUpperCase().replace(/[^A-Z]/g,'').slice(0,6);pairLabel.textContent=p.length===6?`${p.slice(0,3)} / ${p.slice(3)}`:'PAIRE FX'}symbol.addEventListener('input',updatePair);document.querySelectorAll('[data-pair]').forEach(b=>b.onclick=()=>{symbol.value=b.dataset.pair;updatePair()});document.querySelectorAll('.analyst').forEach(a=>a.querySelector('input').addEventListener('change',()=>a.classList.toggle('active',a.querySelector('input').checked)));function show(which){[empty,progress,report].forEach(x=>x.classList.add('hidden'));which.classList.remove('hidden')}function selectedAnalysts(){return [...document.querySelectorAll('.analyst input:checked')].map(x=>x.value)}async function loadHistory(){const r=await fetch('/api/history');const data=await r.json();currentHistory=data.items||[];historyCount.textContent=currentHistory.length;renderHistory()}function renderHistory(){if(!currentHistory.length){historyList.innerHTML='<div class="history-empty"><span>◌</span><h3>Aucune analyse enregistrée</h3><p>Les analyses terminées apparaîtront ici.</p></div>';return}historyList.innerHTML=currentHistory.map(item=>{const label=item.symbol.replace('=X','');const when=new Date(item.created_at).toLocaleString('fr-FR',{dateStyle:'medium',timeStyle:'short'});return `<article class="history-card" data-id="${item.id}"><div class="history-card-main"><div class="history-pair">${label}<small>${when}</small></div><div class="history-decision">${escapeHtml(item.decision||'REVUE')}</div><p>${escapeHtml((item.reports?.final_trade_decision||item.reports?.trader_investment_plan||'Analyse terminée.').slice(0,220))}</p></div><div class="history-card-actions"><button class="open-history" data-id="${item.id}">Ouvrir</button><button class="delete-history" data-id="${item.id}" aria-label="Supprimer">×</button></div></article>`}).join('');document.querySelectorAll('.open-history').forEach(b=>b.onclick=()=>openHistory(b.dataset.id));document.querySelectorAll('.delete-history').forEach(b=>b.onclick=()=>deleteHistoryItem(b.dataset.id))}function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function openHistory(id){const item=currentHistory.find(x=>x.id===id);if(!item)return;renderReport({payload:{symbol:item.symbol,date:item.date},decision:item.decision,reports:item.reports});document.querySelector('[data-view="new-analysis"]').click()}async function deleteHistoryItem(id){if(!confirm('Supprimer cette analyse de l’historique local ?'))return;await fetch(`/api/history/${id}`,{method:'DELETE'});await loadHistory()}document.querySelectorAll('.nav-button').forEach(button=>button.onclick=()=>{document.querySelectorAll('.nav-button').forEach(x=>x.classList.remove('active'));button.classList.add('active');const history=button.dataset.view==='history';document.querySelector('.workspace').classList.toggle('hidden',history);historyView.classList.toggle('hidden',!history);if(history)loadHistory()});document.querySelector('#clear-history').onclick=async()=>{if(!currentHistory.length||!confirm('Effacer tout l’historique local ?'))return;await fetch('/api/history',{method:'DELETE'});await loadHistory()};form.addEventListener('submit',async e=>{e.preventDefault();if(!selectedAnalysts().length){alert('Sélectionnez au moins un analyste.');return}launch.disabled=true;show(progress);progressTitle.textContent=`Analyse de ${symbol.value.toUpperCase()}`;progressMessage.classList.remove('error');progressMessage.textContent='Préparation de la mission…';try{const r=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({symbol:symbol.value,date:date.value,analysts:selectedAnalysts()})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Impossible de lancer l’analyse');poll(data.job_id)}catch(err){progressMessage.textContent=err.message;progressMessage.classList.add('error');launch.disabled=false}});async function poll(id){clearTimeout(timer);try{const r=await fetch(`/api/jobs/${id}`);if(!r.ok)throw new Error(`Serveur local indisponible (${r.status})`);const job=await r.json();progressMessage.textContent=job.message||'Analyse en cours…';if(job.status==='completed'){launch.disabled=false;await loadHistory();renderReport(job);return}if(job.status==='failed')throw new Error(job.message);timer=setTimeout(()=>poll(id),1800)}catch(err){progressMessage.textContent=err.message||'La connexion avec le serveur local a échoué.';progressMessage.classList.add('error');launch.disabled=false}}function renderReport(job){show(report);document.querySelector('#report-symbol').textContent=`${job.payload.symbol.replace('=X','')} · ${job.payload.date}`;document.querySelector('#decision').textContent=job.decision||job.reports.final_trade_decision||'Analyse terminée.';const tabs=document.querySelector('#report-tabs');const content=document.querySelector('#report-content');const labels={market_report:'Marché',news_report:'Macro & actualités',sentiment_report:'Sentiment',investment_plan:'Débat',trader_investment_plan:'Plan du trader',final_trade_decision:'Risque & décision'};const entries=Object.entries(job.reports||{}).filter(([,v])=>v);tabs.innerHTML='';entries.forEach(([key,value],i)=>{const b=document.createElement('button');b.textContent=labels[key]||key;b.className=i===0?'active':'';b.onclick=()=>{tabs.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');content.textContent=value};tabs.appendChild(b)});content.textContent=entries[0]?.[1]||job.decision||''}loadHistory();
